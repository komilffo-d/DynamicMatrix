using System.Runtime.InteropServices;
using System.Security.Permissions;

namespace DynamicMatrix_WF
{
/*
    public static class DynamicMatrix
    {
        [DllImport(@"DynamicMatrix.dll", EntryPoint = "ConvertToFloatMatrix" )]

        public static extern IntPtr ConvertToFloatMatrix(in int[,] intMatrix, IntPtr rows, IntPtr cols);

        [DllImport(@"DynamicMatrix.dll", EntryPoint = "MultiplicationOnNumberInt", SetLastError = true)]
        public static extern IntPtr MultiplicationOnNumberInt(in int[,] matrix, IntPtr rows, IntPtr cols, int number);

        [DllImport(@"DynamicMatrix.dll", EntryPoint = "SummInt", SetLastError = true)]

        public static extern int SummInt(int one,int two);

    }*/
}
